This is an AIO (All In One) Mining Script for RS3.
The main script class is "src/superMiner/SuperMiner.java".
This script uses a task based framework. (activate, execute).
The task classes are located in the tasks package.
Each class in the mineInfo package contains contains information about a particular mining location.
The gui package contains the class files that are used to create GUIs for this script.